<%= simple_form_for(round, html: {class: 'form-vertical' }) do |f| %>
  <%= f.error_notification %>
  <%= f.input :round_name, autofocus: true, required: true %>
  <%= f.button :submit, class: 'btn-primary' %>
  <%= link_to "Cancel", rounds_path %>
<% end %>