<%= simple_form_for(formulation, html: {class: 'form-vertical' }) do |f| %>
  <%= f.error_notification %>
  <%= f.input :formulation_name, autofocus: true, required: true %>
  <%= f.button :submit, class: 'btn-primary' %>
  <%= link_to "Cancel", formulations_path %>
<% end %>